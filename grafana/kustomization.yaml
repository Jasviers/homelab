apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

# Una entrada por dashboard. Plantilla:
#
#   - name: grafana-dashboard-<nombre>
#     files:
#       - dashboards/<carpeta>/<nombre>.json
#     options:
#       disableNameSuffixHash: true
#       labels:
#         grafana_dashboard: "1"
#       annotations:
#         grafana_folder: "<Carpeta>"
configMapGenerator: []
