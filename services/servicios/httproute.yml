apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: servicios
  namespace: servicios
spec:
  parentRefs:
    - name: homelab
      namespace: envoy-gateway-system
  hostnames:
    - servicios.bonchan.org
  rules:
    - backendRefs:
        - name: servicios
          port: 3000
