apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: proxmox
  namespace: proxmox
spec:
  endpoints:
    - ip:
        address: 192.168.1.3
        port: 8006
  tls:
    insecureSkipVerify: true
