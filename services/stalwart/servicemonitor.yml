apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: stalwart
  namespace: stalwart
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: stalwart
  endpoints:
    - port: http
      path: /metrics/prometheus
      interval: 30s
